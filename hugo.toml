baseURL = "/"
languageCode = "en-US"
defaultContentLanguage = "en"
title = "Revealing urban area from mobile positioning data"
theme = "hugo-story"

relativeURLs = "true"

[params]
  [params.seo]
    title = "Revealing urban area from mobile positioning data"
    author = "Gergő Pintér"
    description = "Is it worth concealing the geographic location of mobility data? Can we say that we have protected users' privacy by doing so? How can the privacy of individuals be ensured in a public mobility data?"
    keywords = ["mobile positioning data", "urban mobility", "privacy", "reverse-engineering"]


[services]
  [services.googleAnalytics]
    id = "G-LKEDBX9YC6"

[privacy]
  [privacy.googleAnalytics]
    disable = false
    respectDoNotTrack = true
